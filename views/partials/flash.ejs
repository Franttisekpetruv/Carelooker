<% if(success && success.length){ %>
    <div class="alert">
        <span> <%=success %> </span>
    </div>
    <% } %>
        <% if(error && error.length){ %>
            <div class="alert">
                <%= error %>
            </div>
            <% } %>